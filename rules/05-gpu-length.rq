PREFIX tdy: <https://tdytrading.example/parts#>

CONSTRUCT { ?gpu tdy:fitsWith ?case }
WHERE {
  ?gpu  a tdy:GPU  ; tdy:lengthMm ?len .
  ?case a tdy:Case ; tdy:maxGpuMm ?max .
  FILTER(?len <= ?max)
}
